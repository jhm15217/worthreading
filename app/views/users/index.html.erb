<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	 <%= render @users if current_user.admin? %>
	 <%= render @users.where(confirmed: true) unless current_user.admin?  %>
</ul>

<%= will_paginate %>